# [R4] ID-коллизии, допущенные намеренно (pack-lint.sh, WP-429, 2026-07-27).
# По одному id: на строку — R4 не блокирует коммит на этих значениях.
# Не трогать без проверки: каждая строка — подтверждённый, не предполагаемый, случай.

# Двуязычная пара canon (ru, role: canon) + snapshot (en, role: snapshot, pending_sync)
# одного жанрового шаблона — общий id по дизайну (WP-412).
LS.GENRE.guide
LS.GENRE.post
